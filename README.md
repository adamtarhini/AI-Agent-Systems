Phase 0: Python and tooling foundations

What I built:

A small CLI that loads config from (.env) , validates it with a Pydantic
model, fires three concurrent HTTP requests with (asyncio.gather), and
prints a typed summary. Under 100 lines.
